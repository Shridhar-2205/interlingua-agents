# Optional — ELP agents work without an LLM (deterministic fallback).
# Set these to enable LLM-enhanced ToM (richer rationale + grounding).
LLM_BASE_URL=https://litellm.prod.outshift.ai
LLM_API_KEY=sk-REPLACE_ME
LLM_MODEL=bedrock/global.anthropic.claude-opus-4-6-v1
